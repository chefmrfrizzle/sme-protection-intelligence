# The deterministic demo requires no secrets.
DEMO_MODE=true
AI_MODE=replay
PERSISTENCE_MODE=demo
NEXT_PUBLIC_PRODUCT_WORDMARK=[PRODUCT]

# Server-only production adapter placeholder. Never commit a populated value.
DATABASE_URL=
